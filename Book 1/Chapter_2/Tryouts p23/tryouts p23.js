// Initialize the variable 'balloons' with a value of 100
var balloons = 100; 

// The '*=' operator multiplies the current value by the number on the right
// This is shorthand for: balloons = balloons * 2;
balloons *= 2;

// The result is now 200
console.log(balloons);